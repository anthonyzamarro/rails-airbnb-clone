<% if registration_signed_in? %>
<%= render 'shared/banner_login'  %>
    <% else %>
    <%= render 'shared/banner_logout'  %>
    <div class="container">
      <div class="row_main">
    <div class="col-xs-12 col-sm-6 col-md-offset-3">
      <%# image_tag "paw-print-logo.svg", height: 200, :class => "img_main_log_out" %>
      <h1 class="header_main">
      Dogs ' R ' Us
      </h1>
      <h3 class="sub_header_main">
      Borrow a dog with
      <br> us in just 3 steps!
      </h3>
      <br>
      <%= link_to "Sign up", new_registration_registration_path, :class => "sign_up_button btn btn-primary" %>
      <%= link_to "Log in", new_registration_session_path, :class => "sign_in_button btn btn-primary" %>
    </div>
    <% end %>
  </div>
</div>

<%= simple_form_for @dog do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :species, label: "Breed" %>
  <%= f.input :photo, as: :attachinary %>

  <%= f.button :submit %>
<% end %>
